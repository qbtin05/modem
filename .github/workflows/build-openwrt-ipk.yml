# Reverting to the previous version

This is a reverted state of the .github/workflows/build-openwrt-ipk.yml file. The file is rolled back to the last known stable version before recent changes.

# Actions

- Modified the way the IPK is built
- Updated dependencies
- Adjusted environment variables